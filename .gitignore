# Gradle files
.gradle/
build/

# Local configuration file (sdk path, etc)
local.properties

# Log/OS Files
*.log

# Android Studio generated files and folders
captures/
.externalNativeBuild/
.cxx/
*.aab
*.apk
output-metadata.json

# IntelliJ
*.iml
.idea/
misc.xml
deploymentTargetDropDown.xml
render.experimental.xml

# Keystore files
*.jks
*.keystore
# The base64 form scripts/make-upload-keystore.sh writes for pasting into
# GitHub secrets — it is the private key in text form, so it must never be
# committed just because it has a .txt extension.
*.base64.txt

# Google Services (e.g. APIs or Firebase)
google-services.json

# Android Profiling
*.hprof
